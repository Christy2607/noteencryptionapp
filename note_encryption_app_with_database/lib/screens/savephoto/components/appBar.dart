import 'package:flutter/material.dart';

AppBar appBarSavePhoto() {
  return AppBar(
    leading: const Icon(Icons.arrow_back),
    title: const Text("Save Photo"),
    backgroundColor: const Color.fromARGB(255, 0, 48, 73),
  );
}
